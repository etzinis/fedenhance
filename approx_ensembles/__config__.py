from os.path import expanduser
import os
# Save directory paths
home_folder = expanduser("~")
other_tree_path = ''
MNIST_LOG_PATH = os.path.join(home_folder, other_tree_path,
                              'approx_ensembles_log', 'mnist')

# Datasets paths
MNIST_PATH='/mnt/data/mnist'
